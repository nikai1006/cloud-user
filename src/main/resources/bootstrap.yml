spring:
  application:
    name: nikai-user
  cloud:
    config:
      enabled: flase  #启用配置中心
      discovery:
        enabled: flase #启用配置发现
        service-id: CONFIG #配置中心服务名
eureka:
  instance:
    non-secure-port: ${server.port}
    secure-port: ${server.port}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册中心服务器
